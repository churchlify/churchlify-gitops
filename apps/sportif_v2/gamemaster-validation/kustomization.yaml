apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Operator-run only. Deliberately excluded from ../kustomization.yaml so ArgoCD
# does not recreate a completed Job or own its ephemeral cleanup input Secret.
namespace: sports-tracking

resources:
  - job.yaml