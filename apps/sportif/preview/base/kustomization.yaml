apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sports-tracking

resources:
  - deployment-preview-worker.yaml

# Note: SRS and MediaMTX are managed by sport-track application
# Sportif's preview-worker consumes streams from sport-track's media stack

commonLabels:
  app: preview-worker
  component: preview
  part-of: sportif
