apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sports-tracking

resources:
  - ../../../shared/namespace.yaml
  - deployment.yaml

commonLabels:
  app: cleanup-worker
  component: worker
  part-of: sportif
