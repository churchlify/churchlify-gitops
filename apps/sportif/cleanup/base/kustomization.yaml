apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sports-tracking

resources:
  - deployment.yaml

labels:
  - pairs:
      app: cleanup-worker
      component: worker
      part-of: sportif
