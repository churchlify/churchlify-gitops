apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-worker
  namespace: sports-tracking
  labels:
    app: notification-worker
    component: worker
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: notification-worker
  template:
    metadata:
      labels:
        app: notification-worker
        component: worker
    spec:
      serviceAccountName: notification-worker
      terminationGracePeriodSeconds: 30
      containers:
        - name: notification-worker
          image: ghcr.io/agogos-llc/sportif-notification-worker:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            tcpSocket:
              port: 8006
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: notification-worker
  namespace: sports-tracking
  labels:
    app: notification-worker
    component: worker

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: notification-worker
  namespace: sports-tracking
  labels:
    app: notification-worker
    component: worker
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: notification-worker
  namespace: sports-tracking
  labels:
    app: notification-worker
    component: worker
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: notification-worker
subjects:
  - kind: ServiceAccount
    name: notification-worker
    namespace: sports-tracking
