apiVersion: v1
kind: Service
metadata:
  name: tracking-worker-service
  namespace: sports-tracking
  labels:
    app: tracking-worker
spec:
  type: ClusterIP
  selector:
    app: tracking-worker
  ports:
    - name: http
      port: 8000
      targetPort: 8001
      protocol: TCP
