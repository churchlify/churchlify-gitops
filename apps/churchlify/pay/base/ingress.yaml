apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: churchlify-pay-ingress
  namespace: churchlify
  annotations:
    kubernetes.io/ingress.class: nginx 
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - pay.churchlify.com
    secretName: churchlify-pay-tls
  rules:
  - host: pay.churchlify.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: churchlify-pay # Must match your Service name
            port:
              number: 3000          # Match the Service port