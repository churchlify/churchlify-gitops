apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: churchlify-webapp-ingress
  namespace: churchlify
  annotations:
    kubernetes.io/ingress.class: nginx 
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - app.churchlify.com
    secretName: churchlify-webapp-tls
  rules:
  - host: app.churchlify.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: churchlify-webapp # Must match your Service name
            port:
              number: 3000          # Match the Service port