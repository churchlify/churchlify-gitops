resources:
  # Sportif owns SRS, MediaMTX, their services, and shared RWX storage.
  # This legacy manifest keeps only sport-track consumers.
  - deployment-recorder.yaml

  # Processing Workers
  - deployment-tracking.yaml
  - deployment-stitch.yaml
  - deployment-reel.yaml

  # Network & Storage
  - service.yaml
  - service-ai.yaml
  - ingress.yaml
  - nginx-patch.yaml
